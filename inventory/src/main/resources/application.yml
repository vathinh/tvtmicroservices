server:
  port: 8082
spring:
  application:
    name: inventory
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/inventory
    username: tvt
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true